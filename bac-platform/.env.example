# Optional GitHub material-source configuration for deployed environments.
GITHUB_OWNER=moudabouacha09-lab
GITHUB_REPOSITORY=The-Ultimate-Bac-Help
GITHUB_BRANCH=main

# Required only if the repository is private. Create a fine-grained token with
# read-only Contents access, then configure this value in Vercel (never commit it).
# GITHUB_TOKEN=
